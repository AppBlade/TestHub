# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  global:
    hello: Hello, %{name}
    install: Install
    unregistered: This is an unregistered device.
    app:
      name: TestHub
      slogan: Give your mobile applications wings
    navigation:
      menu: Menu
    device:
      registration: Register this device to enable downloads.
    github:
      track: Track more repositories
      track_save: Track the selected repositories
      registration: Register with GitHub to add repositories.
  devices:
    scep:
      cn: TestHub Registration
    new:
      organization: TestHub
      display_name: TestHub Registration
      description: TestHub uses this profile to establish the identify of this device.
  ipa:
    errors:
      no_payload: '"/Payload" does not exist'
      no_package: '"/Payload" does not contain a package'
      multiple_packages: '"/Payload" contains multiple packages'
      no_codesign: "%{package_name} has not been codesigned"
      missing_info_plist: "%{package_name} is missing the Info.plist file"
  bundle:
    errors:
      armv7_thin_target_error: ARMv7-thin builds require a minimum os target of 4.0 or higher
      required_capabilities: requires %{missing_capabilities}
      forbidden_capabilities: forbids %{forbidden_capabilities}
      ipad_only: requires an iPad
      not_provisioned: is not provisioned
      minimum_os_not_met: requires iOS %{minimum_operating_system}
      expired: provisioning profile has expired
      capability_target_error: setting %{capability_target_errors} in UIRequiredDeviceCapabilities requires either an ARMv6/7 fat binary or minimum os target of 4.3 or higher
    capabilities:
      telephony: Phone.app
      wifi: the ability to connect to Wifi
      sms: Messages.app
      still-camera: the presence of a camera
      auto-focus-camera: the presence of a camera with auto-focus capabilities
      front-facing-camera: the presence of a front-facing camera
      camera-flash: the presence of a camera flash
      video-camera: the presence of a video camera
      accelerometer: the presence of an accelerometer
      gyroscope: the presence of a gyropscope
      location-services: the ability to retrieve the device’s current location
      gps: the presence of GPS hardware
      magnetometer: the presence of a magnetometer
      gamekit: Game Center
      microphone: the presence of a microphone
      opengles-1: an OpenGL ES 1.1 compatible device
      opengles-2: an OpenGL ES 2.0 compatible device
      armv6: an ARMv6-compatible device
      armv7: an ARMv7-compatible device
      peer-peer: peer-to-peer connectivity over Bluetooth
      bluetooth-le: the presence of Bluetooth low-energy hardware
